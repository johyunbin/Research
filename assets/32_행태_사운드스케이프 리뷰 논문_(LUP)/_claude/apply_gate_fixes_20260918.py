# -*- coding: utf-8 -*-
"""
Paper32 — 독립 게이트(fresh 에이전트, 2026-09-18 18시 KST) 지적 반영

게이트 지적 16건 중 원자료로 확인한 것만 고친다. 확인 근거:
- 등록본 `osf_registration_draft_EN_20260802_211715.md` 116–118행: "≥3 comparable effect sizes (anticipated: walking speed;
  dwell time/lingering), using Hedges' g or correlation r" — 다섯 도메인은 서술 종합의 틀이지 메타분석 클러스터 정의가 아니다.
  등록본 34행: OpenAlex 는 2026-08-02 실행(스크리닝은 08-06, deviation_log D6-2).
- `fulltext/analysis_rules.md` §1: OR 변환은 χ²(1)·2×2 빈도만 · §4: "관측 n밖에 없으면 민감도 전용".
  그런데 `ma/ma_staying_input.csv` 1280 = 연속 지수 보정 로지스틱 OR 의 Chinn 변환, `ma/ma_social_input.csv` 931·1069 = 발췌 단위 n
  → 규칙 이탈 2건을 2.7·2.9·4.6 에 밝힌다.
- `ma/ma_sensitivity_v2_raw.json` MA3 LOO p = 0.059·0.066·0.041·0.057 → 4.2 의 "한 편에서 나온 증분" 서술을 4.6·3.5 와 맞춘다.
- 재판정 일치도: 두 범주(포함·민감도용 대 배제) 46/50·κ 0.84, 세 범주 45/50 — 두 번째 평가도 AI 보조임을 밝힌다.
- 917 Su et al. 2023 = setting "VR-lab"(corpus_v4_extraction) · 1069 = 두 사람 간(paired) 상호작용 · D5-5 CT0007↔761 = 합성 기여 0편.
- OSF 갱신은 아직 입력 전 → 2.9 끝 문장을 완료형으로 쓰지 않는다.
"""
import csv, os, sys
from collections import Counter

sys.stdout.reconfigure(encoding="utf-8")
BASE = os.path.dirname(os.path.abspath(__file__))
FT = os.path.join(BASE, "fulltext")
P = os.path.join(os.path.dirname(BASE), "01_논문작업", "Manuscript_KO.md")
t = open(P, encoding="utf-8").read()


def rep(a, b):
    global t
    n = t.count(a)
    if n != 1:
        sys.exit(f"STOP count={n}: {a[:80]}")
    t = t.replace(a, b)


# ── 재판정 일치도(두 범주·세 범주) ─────────────────────────────────
pairs = []
for tag in ("", "_b"):
    for r in csv.DictReader(open(os.path.join(FT, f"newft_final{tag}", "verdict.csv"), encoding="utf-8-sig")):
        if r["recheck_verdict"]:
            pairs.append((r["first_verdict"], r["recheck_verdict"]))
n = len(pairs)
two = lambda v: "exc" if v == "FINAL_EXCLUDE" else "inc"
agree2 = sum(two(a) == two(b) for a, b in pairs)
agree3 = sum(a == b for a, b in pairs)
ca, cb = Counter(two(a) for a, _ in pairs), Counter(two(b) for _, b in pairs)
pe = sum(ca[l] * cb[l] for l in ("inc", "exc")) / n ** 2
kappa = (agree2 / n - pe) / (1 - pe)
split3 = n - agree3
print(f"재판정 {n} · 두 범주 일치 {agree2} ({agree2/n:.0%}) κ={kappa:.2f} · 세 범주 일치 {agree3} · 갈린 {split3}")
assert (n, agree2, agree3) == (50, 46, 45)

# ── 2.4 ───────────────────────────────────────────────────
rep("전문 검토에서 반복적으로 나타난 경계 사례는 판정 규칙을 정해 해당 레코드 전체에 같은 방식으로 적용했다.",
    "전문 검토에서 반복적으로 나타난 경계 사례는 판정 규칙을 정해 해당 레코드 전체에 같은 방식으로 적용했다(예외는 2.9절).")
rep(f"배제 연구를 합한 {n}편은 첫 판정을 참조하지 않은 두 번째 평가로 다시 판정했으며, "
    f"포함 여부에 대한 두 판정의 일치율은 {agree2/n:.0%}(Cohen's κ = {kappa:.2f})였다. 판정이 갈린 {split3}편은 원문을 근거로 조정했다.",
    f"배제 연구를 합한 {n}편은 첫 판정을 참조하지 않은 두 번째 AI 보조 평가로 다시 판정했으며, "
    f"포함 또는 민감도 분석용 대 배제의 일치율은 {agree2/n:.0%}(Cohen's κ = {kappa:.2f})였다. "
    f"세 판정 범주 기준으로 결과가 갈린 {split3}편은 원문을 근거로 조정했다.")

# ── 2.7 ───────────────────────────────────────────────────
rep("표본을 공유하는 연구는 한 편만 포함했고, 반복 관측 설계에서는 관측 수가 아니라 참가자 수를 표본 크기로 사용했다.",
    "표본을 공유하는 연구는 한 편만 포함했고, 반복 관측 설계에서는 관측 수가 아니라 참가자 수를 표본 크기로 사용했다. "
    "다만 참가자 수를 보고하지 않은 사회적 상호작용 연구 2편은 영상 발췌 단위를 표본 크기로 사용했다(2.9절).")

# ── 2.9 첫 문단 ─────────────────────────────────────────────
rep("인용 추적은 8월 선별 시점의 포함 연구를 시드로 한 차례만 수행했다. 등록한 OpenAlex 보조 검색은 인용 추적을 마친 뒤인 2026년 8월 6일에 수행했다.",
    "인용 추적은 8월 선별 시점에 포함되거나 민감도 분석용으로 분류된 84편을 시드로 한 차례만 수행했다. "
    "등록한 OpenAlex 보조 검색은 2026년 8월 2일에 실행했으나, 그 결과는 인용 추적을 마친 뒤인 8월 6일에 스크리닝했다.")
rep("민감도 분석 전용으로 분류하는 규칙은 인용 추적 경로에만 적용됐다. 이러한 연구는 어느 메타분석에도 포함되지 않았다.",
    "민감도 분석 전용으로 분류하는 규칙은 인용 추적 경로에만 적용됐으며, 이 규칙으로 분류된 연구는 어느 메타분석에도 쓰이지 않았다.")

# ── 2.9 둘째 문단(메타분석) ───────────────────────────────────
a = t.index("등록본은 보행속도와 체류의 합성만 예상했으나,")
b = t.index("고정효과 모형과의 비교와 하위그룹 분석은", a)
b = t.rindex("등록한 ", a, b)
t = t[:a] + (
    "등록본은 비교 가능한 효과크기가 3개 이상인 클러스터를 Hedges' *g*나 상관계수로 합성하도록 하고 보행속도와 체류를 예상했다. "
    "효과크기를 산출하는 단계에서 이 기준에 따라 사회적 상호작용 클러스터와, 여러 행태 도메인과 두 방향의 상관을 함께 모은 "
    "소리–행태 상관 클러스터를 추가로 구성했다. 방문·공간이용 결과는 분산 정보가 없어 합성하지 못했고, 그 밖의 결과는 비교 가능한 "
    "효과크기가 3개에 이르지 않아 서술 종합으로 다뤘다. 분석 규칙은 효과크기를 계산하기 전에 정했으나 두 가지는 규칙과 다르게 처리했다. "
    "규칙에 없던 로지스틱 회귀의 보정 오즈비 변환을 체류 클러스터의 한 연구에 적용했고, 관측 단위만 보고한 연구는 민감도 분석에만 "
    "쓰기로 한 규칙과 달리 사회적 상호작용 연구 2편은 영상 발췌 단위를 표본 크기로 해 주분석에 포함했다. "
    "보행속도 분석의 광장 배경음악 연구와 사회적 상호작용 분석의 비독립 관찰 연구는 주분석에서 제외하고, 이들을 포함한 결과는 "
    "민감도 분석으로 보고했다(3.5절). 체류 분석에 방문빈도 결과를 포함한 분석, 비독립 관찰 연구를 포함한 분석, 9월에 추가로 확보한 "
    "연구 가운데 클러스터 정의를 일부만 충족한 효과를 더한 분석은 주분석을 마친 뒤 추가했다. 9월 추가 연구 가운데 클러스터 정의를 "
    "충족해 주분석에 들어간 효과는 1건이었다. ") + t[b:]
rep("등록본은 이러한 변경을 반영해 갱신했고(2026년 9월), 전체 편차 기록은 OSF 프로젝트에 공개한다.",
    "이러한 변경은 OSF 등록본의 갱신 기록(2026년 9월)에 반영하며, 전체 편차 기록은 OSF 프로젝트에 공개한다.")

# ── 3.5 ───────────────────────────────────────────────────
rep("결과가 행동 기대로 측정돼 민감도 분석 전용으로 분류한 실험 연구를 더하면",
    "결과가 행동 기대로 측정돼 민감도 분석 전용으로 분류한 가상현실 실험실 연구를 더하면")

# ── 4.2 ───────────────────────────────────────────────────
rep("첫째, 유의한 사회적 상호작용 클러스터를 자연음을 더한 효과나 소음을 없앤 효과 중 어느 하나로만 해석해서는 안 된다.",
    "첫째, 유의한 사회적 상호작용 클러스터를 자연음을 더한 효과나 소음을 없앤 효과 중 어느 하나로만 해석하기는 어렵다.")
rep("또한 이 클러스터를 유의 수준 너머로 끌어올린 증분은 인용 검색으로 확보한 기계음 실험 한 편에서 나왔다.",
    "또한 이 클러스터의 유의성은 개별 연구에 민감해, 인용 추적으로 확보한 기계음 실험을 포함한 네 연구 가운데 세 연구는 "
    "각각 제외했을 때 *p*가 0.05를 넘었다(3.5절).")

# ── 4.3 ───────────────────────────────────────────────────
rep("소음 저감을 신체활동을 늘리는 보편적인 방법으로 보는 관점도 코퍼스의 관찰 자료로는 뒷받침되지 않았다. "
    "헬싱키 가로 13,322개 구간의 달리기 기록을 분석한 연구에서는",
    "소음 저감을 신체활동을 늘리는 보편적인 방법으로 보기에도 코퍼스의 근거는 엇갈렸다. "
    "헬싱키 가로 13,322개 구간의 달리기 기록을 분석한 대규모 관찰 연구에서는")

# ── 4.6 ───────────────────────────────────────────────────
rep("판정 일치도는 9월 추가 확보분의 재판정에서만 산출했다.",
    "판정 일치도는 9월 추가 확보분에서 두 AI 보조 평가 사이에서만 산출했으므로 리뷰어 간 신뢰도를 나타내지는 않는다.")
rep("표본을 공유한 논문 묶음이 세 개 있었으며, 각 묶음에서 한 편만 정량 합성에 포함했다.",
    "표본을 공유한 논문 묶음이 세 개 있었으며, 각 묶음에서 많아야 한 편만 정량 합성에 포함했다.")
rep("사회적 상호작용 클러스터는 공원과 주거단지의 집단 상호작용과 낯선 사람을 돕는 행동을 함께 다뤘고,",
    "사회적 상호작용 클러스터는 공원의 집단 상호작용, 주거단지의 두 사람 간 상호작용, 낯선 사람을 돕는 행동을 함께 다뤘고,")
rep("체류 클러스터의 한 연구는 연속형 자연음 지수의 보정 오즈비를 효과크기로 변환한 것이다.",
    "체류 클러스터의 한 연구는 연속형 자연음 지수의 보정 오즈비를 효과크기로 변환한 것이며, 사회적 상호작용 클러스터의 두 연구는 "
    "참가자 수 대신 영상 발췌 단위를 표본 크기로 써서 정밀도가 과대평가됐을 수 있다.")

open(P, "w", encoding="utf-8", newline="").write(t)
print("OK — 게이트 지적 반영")

# ── 부록 B: 본문 인용 연구의 연도를 참고문헌(권호 연도)과 일치 — build_appendix.py 재생성본으로 교체 ──
#   (게이트 범위 밖 지적: Montes González Table 2·[43] 2023 ↔ 부록 B 2022 · 같은 원인 941 Ba et al. 2023 ↔ [48] 2024)
t = open(P, encoding="utf-8").read()
gen = open(os.path.join(FT, "appendix_ab.md"), encoding="utf-8").read().replace("\r\n", "\n").strip("\n")
assert gen.startswith("## Appendix A") and "Montes González et al. (2023)" in gen
a = t.index("## Appendix A")
t = t[:a] + gen + "\n"
open(P, "w", encoding="utf-8", newline="").write(t)
print("OK — 부록 A·B 재생성본 교체")
