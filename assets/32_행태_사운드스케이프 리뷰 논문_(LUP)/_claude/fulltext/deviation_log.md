# Paper32 — 프로토콜 이탈·사후 결정 기록 (Protocol deviation log)

OSF 사전등록 **osf.io/7ew8q**(Generalized Systematic Review Registration) 대비, 실행 과정에서
등록서에 없던 결정을 내렸거나 등록 내용을 변경한 사항을 전부 기록한다. PRISMA 2020 item 24c
(등록 대비 변경사항 보고) 이행이며, 투고 원고 Methods 말미 또는 Supplementary에 그대로 싣는다.

원칙: **숨기지 않고 사유와 함께 기록한다.** 결과에 유리하게 사후 조정한 것이 아니라,
등록 시점에 예상하지 못한 자료 구조에 대응한 것임을 각 항목에서 밝힌다.

---

## D1. 검색·선별

### D1-1. BORDERLINE 189건 전건 전문심사 (등록 범위 유지)
- **등록**: 제목·초록 스크리닝 후 적격 후보를 전문심사.
- **실행**: 이중 스크리닝에서 BORDERLINE으로 남은 189건을 배제하지 않고 전건 전문심사로 넘겼다.
- **사유**: 사운드스케이프-행태 연구는 초록에 행태 아웃컴을 명시하지 않는 관행이 있어(측정은
  했으나 초록에는 지각 결과만 서술) 초록 단계 배제의 위음성 위험이 크다.
- **영향**: 포함 편수 증가 방향. 선별 기준을 느슨하게 바꾼 것이 아니라 판단 시점을 뒤로 미룬 것이다.

### D1-2. 경계 사례 판정 3원칙 (사후 정식화)
등록서는 PICO 수준의 기준만 명시했고, 전문심사에서 반복 등장한 세 유형에 대해 판정 원칙을
사후 명문화했다. **원칙 수립 시점은 개별 판정 이전**이며, 이후 일괄 적용했다.
| 원칙 | 내용 | 처리 |
|---|---|---|
| P1 | 실내 재현(랩)이되 옥외 장면을 자극으로 쓴 연구 | 민감도 전용(SENS_ONLY) |
| P2 | 행태 아웃컴이 자기보고 의도(intention)에 그치는 연구 | 배제 |
| P3 | 음환경이 통제변수로만 들어간 연구 | 배제 |
- **사유**: 등록 시점에 이 세 경계의 빈도를 예측하지 못했다. 원칙 없이 개별 판단하면 판정이
  평가자에 따라 흔들린다.
- **영향**: SENS_ONLY 3편이 주분석에서 제외되고 민감도로 분리됐다.

### D1-3. 인용추적 산출물에 대한 자동 우선순위 필터
- **등록**: *"Backward and forward citation tracking of all included studies."* (필터 방식 미명시)
- **실행**: OpenAlex로 수집한 신규 후보 2,073건(backward 413 · forward 1,660)에 대해,
  **등록된 3블록 검색 논리(음환경 × 행태 × 옥외세팅)를 제목에 적용**해 우선순위를 매기고
  T1(11건) + T2(417건) = 428건을 제목 스크리닝 대상으로 삼았다. T3(동물·생태음향 26건)과
  T4(3블록 중 2블록 이상 미충족 1,619건)는 제목 수준에서 자동 배제했다.
- **사유**: 인용추적은 본검색과 달리 주제 특이도가 낮아(포함 논문의 참고문헌에는 통계기법·
  녹지건강·도시계획 일반이 대량 포함) 전건 수기 스크리닝의 비용 대비 산출이 극히 낮다.
- **한계 명시**: 자동 필터는 제목만 사용하므로, 제목에 행태 어휘가 없는 적격 연구를 놓칠 수 있다.
  본검색은 제목·초록·키워드를 모두 사용했으므로 이 위험은 인용추적 경로에 한정된다.
- **재현성**: 필터 규칙과 T1~T4 배정 결과는 `citation_tracking_triaged.csv`에 전건 보존.

### D1-4. 인용추적 갈래의 미완 상태 (투고 전 반드시 해소)
- 428건 제목 스크리닝 → 146건 초록 스크리닝 → **79건이 전문 확보 대상**으로 남았고, 이 중
  자동 회수된 것은 8건(오픈액세스)뿐이다. 나머지 71건은 출판사 봇 차단(HTTP 403)으로 자동 회수가
  불가능하며, 기관 구독 브라우저에서 직접 내려받아야 한다(`ct_download_worklist.md`).
- **✅ 2026-08-06 해소.** 71건을 기관 구독으로 직접 회수해 54편을 전문 심사했고(포함 15편),
  PRISMA 흐름도(Fig 1)는 3갈래 완전 표기로 갱신했다. 최종 코퍼스 98편.
- 초록 스크리닝에서 39건이 **초록 자체를 기계적으로 확보할 수 없어**(Elsevier 등이 OpenAlex·Crossref·
  Semantic Scholar 어디에도 초록을 싣지 않음) 제목만으로 판단해야 했다. 이 39건은 배제하지 않고
  전문 확보 대상에 포함했다 — 판단 불가를 배제 근거로 삼지 않는다는 원칙.

---

## D2. 품질평가

### D2-1. MMAT 범주 배정을 전문 판독 결과로 교체
- **등록**: *"MMAT 2018 across heterogeneous designs; RoB 2 for randomized experimental subsets."*
- **실행**: 추출표의 design 문자열로 사전 배정한 MMAT 범주(`mmat_assignment.csv`)와, 전문을 읽은
  평가자가 판정한 범주가 다수 불일치해 **전문 판독 결과를 정본으로 채택**했다.
- **대표 유형**: ①"객관 음향 실측 + 주관 설문"을 mixed methods로 오분류(둘 다 정량이므로
  quantitative에 해당) ②조건 배정이 무작위가 아닌 현장실험을 RCT로 오분류 ③사례연구 기술을
  qualitative로 오분류.
- **영향**: 범주가 바뀌면 적용 문항이 바뀌므로 등급에 영향을 준다. 재배정 내역은
  `quality_all.csv`(정본)와 `mmat_assignment.csv`(사전 배정)의 대조로 확인 가능.

### D2-2. RoB 2 별도 적용 생략
- **등록**: 무작위 실험 하위집합에 RoB 2 병행.
- **실행**: 전문 판독 결과 **진정한 무작위 배정 실험은 2편**(MMAT quantitative RCT)에 그쳐,
  별도 RoB 2 도구를 병행 적용해도 하위집합 분석이 성립하지 않는다. MMAT 2.1~2.5(무작위 범주)로
  대체하고, 두 편의 배정·눈가림 관련 취약점은 서술로 보고한다.
- **사유**: 도구를 추가해도 새로운 정보가 생기지 않으며, k=2로는 "RCT만" 민감도 자체가 불가능하다.

### D2-3. ID 7(1978년 스캔본) 범주 미확정
- 텍스트 레이어가 없는 스캔 PDF로 MMAT 스크리닝 문항 판정이 불가능해 `undetermined`로 남겼다.
  MA에는 기여하지 않으며, 품질 요약의 분모(84)에는 포함하되 등급 집계에서 분리 표기한다.

---

## D3. 메타분석

### D3-1. 클러스터 5개 → 4개 축소
- **등록**: 행태 아웃컴 유형별 클러스터 계획(이동·체류·방문/이용·사회적 상호작용·신체활동).
- **실행**: **방문/공간이용 클러스터는 풀링 가능한 효과크기가 k<2**여서 서술 종합으로 이관하고,
  최종 4클러스터(보행속도·체류·사회적 상호작용·지각–행태 상관)로 풀링했다.
- **사유**: 해당 연구들이 이용량을 빈도·비율로만 보고하고 분산 정보를 싣지 않았다.
  분모(조건별 n)가 없어 OR 변환도 불가능(`source_flags.md §B`).

### D3-2. 규약 문서(`analysis_rules.md`)의 사전 작성
- 등록서의 Synthesis plan은 지표·모델 수준까지만 기술돼 있어, **계산 착수 전** 세칙
  (다중효과 선택 우선순위·유사반복 시 n 규칙·변환 불가 처리)을 별도 문서로 확정했다.
  등록 이후 추가된 규칙이지만 **결과를 보기 전에** 확정했다는 점을 기록해 둔다.

### D3-3. 원문이 보고하지 않은 값의 복원 3건
| ID | 복원 내용 | 근거 | 민감도 |
|---|---|---|---|
| 532 | 군당 n 미보고 → 총 N 균등분할 가정 | 3군 설계 명시 | ⑤에서 제외 후 재계산 |
| 665 | SD 미보고 → 사후 p=0.003에서 t 역산 | 규약 §1 test_stat 경로 | ⑤에서 제외 후 재계산 |
| 323 | EMM±SE → SD 환산(SD=SE·√n) | 표에 n 보고됨 | 해당 없음(표준 환산) |
- 세 건 모두 `ma_*_input.csv`의 `note`·`source` 필드에 복원 경로를 명시했다.

### D3-4. 이질성 점검 중 발견한 자체 오류의 정정 (투명성 기록)
- 사회적 상호작용 클러스터 최초 계산에서 I² = 93.6%가 나왔고, 원인을 추적한 결과 **Moser(1988)
  2×2 표의 오즈비 부호를 반대로 코딩한 내 실수**였다. 정정 후 I² = 43.5%.
- 결과 조정이 아니라 계산 오류 수정이며, 이질성 지표가 오류 탐지 장치로 작동한 사례로 기록한다.

### D3-5. 민감도 분석 실행 범위
등록된 5축을 전부 실행했다(`ma_sensitivity.md`). 다만 두 축은 자료 제약으로 부분 실행이다.
- **③ 관측 n 재계산**: MA1의 617만 재계산 가능(참가자 n·관측 n 모두 보고). MA3의 931·1069는
  발췌·그룹 단위 n만 보고돼 참가자 n으로 되돌릴 수 없어 서술로만 표기.
- **② 저품질 제외**: MA1은 기여 효과 4건이 **전부 MMAT low 연구**(461·532·617)에서 나와,
  low를 제외하면 **k=0으로 풀링 자체가 성립하지 않는다**. 은폐하지 않고 결과의 핵심 한계로 보고한다
  — 이 문헌에서 가장 널리 인용되는 "소음이 보행을 빠르게 한다"는 주장이 방법론적으로 가장 취약한
  근거 위에 서 있다는 뜻이다.

### D3-6. MA1 풀 정의 — 음악 자극 연구(481) 제외
- **실행**: 보행속도 클러스터 주분석은 '자연음 vs 교통·도시소음' 대비 4건(k=4)으로 한정하고,
  Meng(2018, ID 481)의 음악 vs 무음악 대비는 제외했다.
- **사유**: 노출(음악=부가음)과 행태(통과보행이 아니라 매장가 배회, walking-around)가 모두 다르다.
- **방향 확인**: 481은 g = −1.661로 절대값이 가장 크고 부호는 주분석과 같다. 포함하면 추정치가
  −0.474 → −0.773으로 커지고 p도 0.326 → 0.145로 내려간다. 즉 **제외는 보수적 방향**이며,
  결과를 유리하게 만들기 위한 선택이 아니다(민감도 ⑥에 수치로 제시).
- **수치 정정 (2026-09-16)**: 종전 기재값 −0.500 → −0.742(p 0.179 → 0.082)는 D7-2 이전 값이었다.
  민감도 ⑥ 축이 주분석과 다른 로더로 입력을 읽어 532 의 두 실험을 따로 넣고 있었기 때문이다(k=5).
  주분석과 같은 논문 내 합성을 적용해 재계산했고(k=4) 위 값으로 바꿨다. 결론(제외 = 보수적 방향)은 불변.

---

## D5. 인용추적 갈래 실행에서 생긴 결정 (2026-08-03 후반)

### D5-1. 배제 코드 X7(언어 기준 미충족) 신설 — 판정 1건 변경
- 등록대로 영어 학술지만 포함하는데, **OpenAlex가 영문 제목·초록 메타데이터를 제공하는 중국어
  게재지**가 있어 제목·초록 스크리닝에서 걸러지지 않았다. CT0410(风景园林 *Landscape Architecture*)이
  전문 심사에서 드러났다.
- 기존 배제 코드 X1~X6에 언어 항목이 없어 **X7을 신설**해 기록했다. 기록 = `ct_corrections.md`.

### D5-2. P2(의도 아웃컴) 처리 방향 불일치 — 판정 영향 없음
- 인용추적 심사 지시의 P2는 "의도에 그치면 **배제**"였는데, 본검색 갈래의 R2는 "행동의향 →
  **SENS_ONLY**"였다. 지시문 작성 시의 불일치다.
- **확인 결과 실제 판정에는 영향이 없었다** — P2가 언급된 4건(CT0021·CT0063·CT0296·CT0328)은
  모두 다른 기준이 먼저 결정적이었다(X4 또는 X3). 향후 재현 시 **R2를 정본**으로 삼는다.

### D5-3. ★전문 추출 절단이 품질평가를 왜곡 — 전건 재평가
- 1차 심사 패킷은 논문당 **46,000자 상한**을 뒀는데, 인용추적 포함 16편 중 **13편이 초과**했다
  (최장 96,472자). MMAT는 *"보고가 없으면 CT"* 로 판정하는 도구이므로 **추출 한도가 그대로
  등급을 깎는 인공물**이 된다.
- 절단 없는 전문으로 **13편 전건 재평가** → 7편에서 판정이 바뀌었고 **4편은 등급이 이동**했다
  (CT0090·CT0166·CT0335는 각 2등급 상향, CT0348은 1등급 상향, CT0184는 범주 오배정 교정으로 하향).
- 재평가 후 인용추적분 품질(high 25%)이 DB 검색분(high 24%)과 사실상 같아졌다 —
  **"인용추적은 주변부 문헌만 긁어온다"는 인상은 절단 인공물이었다.**
- 대조표 = `quality_truncation_effect.md`. **전문 기준 재평가가 정본**이다.
- 자동 전문 추출을 쓰는 리뷰가 품질평가에서 체계적 하향 편의를 가질 수 있음을 보이는 사례라
  Methods 또는 Limitations에 보고한다.

### D5-4. 메타분석 클러스터 편입 판단 4건
등록 규약(`analysis_rules.md`)의 클러스터 정의를 지키기 위해, 신규 효과를 **기계적으로 넣지 않고**
정의 적합성을 따졌다. 판단과 그 영향을 모두 공개한다.

| 신규 | 판단 | 사유 | 넣었다면 |
|---|---|---|---|
| CT0025 | **MA3 포함** | Moser 1988과 동일 설계·동일 변환경로(2×2 → OR → Chinn d) | — |
| CT0126·CT0184 | **MA4 포함** | 둘 다 r 계열, 프레임 정합 | — |
| CT0175 | MA2 제외 | 아웃컴이 체류시간이 아니라 **방문빈도** | p .074 → .327, I² 54→76% |
| CT0414 | MA3 제외 | 대비가 '청각공간 vs 시각공간'이고 독립성 위배(합계 2,249 > 관측 1,167) | p .021 → .005, I² 49→77% |
| CT0137 | MA4 변형 분리 | M±SD를 **d→r로 변환**해야 하는데 규약 §1에 없는 경로 | r +.409 → +.393 |

- ⚠️ **CT0414는 넣으면 결과가 좋아지는데도 뺐다.** 유리해진다는 이유로 편입하지 않았음을 명시한다.
- CT0137의 d→r 변환은 Borenstein 표준이지만 **등록 규약 밖**이므로 주분석이 아니라 변형분석으로
  분리했고, 넣어도 결론이 같음(r +0.393, p 0.001)을 함께 보고한다.

### D5-5. 동일 표본 중복 2건 — 풀링에서 한 편만 채택
- **CT0126 ↔ CT0322**: 같은 Cáceres 보행자 105명 데이터의 두 논문. **CT0126 채택** —
  분석단위(`sampling point N = 29`)와 집계 절차를 원문 §2.2에 명시하는 반면 CT0322는 n을
  어디에도 밝히지 않아 Fisher-z 분산 산출이 불가능하다.
- **CT0007 ↔ ID 761**: 표본 크기가 정확히 같다(n = 2,034). 별개 논문이므로 서술 종합에는 둘 다
  유지하되 같은 MA 클러스터에는 한 편만 넣는다. 현재 둘 다 풀링에 기여하지 않아 실무 영향은 없다.
- 기록 = `source_flags.md §C-2`.

### D5-6. 결과 서사 수정
- 1차 종합의 핵심 주장은 *"조건 대비형 3개가 전부 CI에 0을 포함 — 관찰·설문 연관만 축적됐고
  실험적 대비는 미축적"* 이었다.
- **MA3가 유의에 도달(p .053 → .021)하면서 이 비대칭 주장은 더 이상 성립하지 않는다.**
  수정된 주장: *"조건 대비 증거의 견고함은 행태 도메인에 따라 갈린다. 사회적 상호작용은 13년 간격의
  독립 반복(Mathews & Canon 1975 · Moser 1988 — **13년 간격**)이 뒷받침해 유의에 도달한 반면, 이동·체류는 여전히
  CI가 0을 포함한다."*
- 결과가 바뀌어 주장을 고친 것이며, 주장을 지키려고 결과를 고르지 않았다.

## D4. 보고

### D4-1. 개념 프레임워크의 위치
- 등록서는 프레임워크 개발을 산출물로 명시했다. 본 리뷰에서는 Results가 아니라 **Discussion의
  종합 절**에 배치하고 Figure로 제시한다. 자료에서 도출한 것이 아니라 자료를 조직하는 틀이므로,
  결과와 해석을 섞지 않기 위한 배치다.

### D4-2. 언어·문헌유형 제한
- 등록대로 **영어 학술지 논문**만 포함했다(중국어 문헌·학술대회 프로시딩 제외). 이는 등록 시점의
  결정이며 이탈이 아니지만, 사운드스케이프 연구에서 중국 기여가 크다는 점에서 결과 해석에
  직접 영향을 주므로 Limitations에 명시한다.

---

## D6. OpenAlex 보조검색 갈래 (2026-08-06)

### D6-1. 랩 재현 규칙의 갈래 간 불일치
- 경계 원칙 P1(옥외 장면을 실험실에서 재현한 연구 → SENS_ONLY)은 **인용추적 갈래 실행 중에
  도입**돼 그 갈래에만 적용됐다. DB 검색 갈래의 VR-lab 5편(661·666·999·1122·1272)은 본분석에
  들어가 있다.
- **정량적 영향 없음** — VR-lab 5편은 **어느 메타분석 풀에도 기여하지 않는다**(전부 서술 종합).
  민감도 축 ⑦로 확인했다(`ma/ma_supplementary.md §3`).
- 재현 시에는 **두 갈래 모두 본분석 포함**(등록 프로토콜 원문)이 정본이다.

### D6-2. 등록 보조 식별원(OpenAlex)의 뒤늦은 전수 이행
- **등록**: OpenAlex를 보조 식별원으로 명시.
- **실행 시점의 문제**: 3-DB 풀에 없는 고유 352건이 **스크리닝되지 않은 채 남아 있었다.**
  자체 감사(`gap_audit.md`)에서 발견해 2026-08-06 전수 이행했다.
- **결과**: 329건 스크리닝 → 15건 확보대상 → 전문평가 5 → **포함 2편**. 둘 다 질적 연구로
  **메타분석 기여 0**. 즉 이 누락이 정량 결과를 바꾸지 않았음이 사후 확인됐다.
- **숨기지 않는 이유**: 등록한 식별원을 빠뜨렸다가 뒤늦게 채운 것은 그 자체가 이탈이다.
  결과가 안 바뀌었다는 사실은 면책이 아니라 **사후에야 알 수 있는 정보**다.

### D6-3. 메타데이터 언어 표기 불신 — 판정 2건 변경
- OpenAlex `language=en` 레코드 중 **실제 본문이 한국어·일본어**인 것이 확보 5편 중 2편이었다
  (영문은 제목·초록뿐). 메타데이터만 믿었으면 등록 언어 기준을 어겼을 것이다.
- 이후 이 갈래의 언어 판정은 **전문 표지 확인**으로만 했다. D5-1(중국어 게재지)과 같은 유형이며,
  색인 메타데이터가 언어·문헌유형 판정 근거가 될 수 없다는 두 번째 사례다.

---

## D7. 독립 통계 검증에서 드러난 자체 오류의 정정 (2026-08-06)

메타분석 구현 전체를 **오류를 찾는 목적으로 독립 코드 검토**한 결과, 네 건의 오류가
드러났다. 전부 우리 잘못이며, 정정 전후 값을 함께 남긴다.

### D7-1. ★ τ² 추정이 REML 이 아니라 ML 이었다 — 전 클러스터 재계산
- 함수 이름은 `reml_tau2` 였고 원고·요약은 "REML"이라고 보고했으나, 실제 고정점은
  **Σwᵢ²(yᵢ−μ̂)² = Σwᵢ** 로 **ML score equation** 이었다. 상수항 모형의 REML 은
  **Σwᵢ²(yᵢ−μ̂)² = Σwᵢ − (Σwᵢ²)/(Σwᵢ)** 여야 한다.
- 같은 함수가 `ma_v2.py`·`ma_supplementary_analyses.py`·`ma_sensitivity_v2.py` **세 곳에
  복제**돼 있었던 것이 원인이다. `ma_core.py` 하나로 통합했다.
- 구 추정치는 참 REML 보다 τ² 를 **MA1 33%·MA2 80%·MA3 68%·MA4 17% 작게** 잡았고,
  그 결과 **예측구간이 실제보다 좁게** 보고됐다.
- **결론은 유지된다** — MA3 는 정확 REML 로도 유의하다(p .0203).
- **전파 누락 정정 (2026-09-16)**: 재계산 뒤에도 원고 §3.4 의 leave-one-out 값이 구 추정치로 남아 있었다
  (MA3 −931 +0.581/p .056 → **+0.556/p .059**, −1069 +0.775/.070 → **+0.768/.066**, −14 +0.803/.043 →
  **+0.801/.041**; MA4 LOO r 범위 .372~.484 → **.387~.477**). "네 번의 제거 중 세 번이 p = .05를 넘는다"는
  서술은 정본값으로도 성립한다. 재발 방지로 `verify_manuscript.py` 에 LOO 대조를 추가했다.

### D7-2. MA1 이 자체 규약 §3(논문당 1효과)을 위반했다 — k 4 → 3
- 532(Franěk 2019)가 Exp1·Exp2 로 **같은 대비프레임에서 두 번** 풀에 들어가 있었다.
  규약 §3 대로 논문 내 평균(효과 간 ρ=0.5 Borenstein)으로 합성했다.
- **g −0.500 (p .179) → −0.474 (p .326).** 불확실성이 커졌고, "보행속도 근거가 가장
  취약하다"는 결론은 오히려 강해진다.
- **후속 적발 (2026-09-16)**: 같은 위반이 민감도 ⑥(음악 연구 포함) 축에 남아 있었다 — 그 축만
  원자료를 직접 읽어 k=5 로 계산했다. 같은 로더로 통일해 k=4, g −0.773, p .145 로 재계산(D3-6 반영).

### D7-3. MA4 입력 2건이 원문과 달랐다 — k 7 → 6
| uid | 문제 | 조치 |
|---|---|---|
| **1018** | 추출표 `effect_stats` 가 **`NR`** 인데 풀링 단계에서 `r=.36, n=310` 을 만들어 넣었다. 원문에 그 조합은 없다(앉은 군 n=310 · 걷는 군 n=556). 아웃컴도 행태가 아니라 만족도다 | **풀에서 제외** |
| **1177** | `n=58` 은 사운드워크 참가자 수이지 상관의 분석단위가 아니다. n=58 이면 p=.0017 로 원문 보고 p=.033 과 어긋난다 | 원문이 보고한 **95% CI [0.05, 0.67] 에서 Fisher-z 분산을 역산**(v=.0377, p=.029 로 정합) |
- **r +0.409 (k=7) → +0.425 (k=6).** 방향·유의성 불변.
- ⚠️ 1018 은 MA4 유일의 MMAT high 였으므로, "② high 만" 민감도는 이제 성립하지 않는다.

### D7-4. `rho 제외` 민감도가 아무것도 제외하지 않았다
- 입력 로드 시 모든 행의 `src` 를 `"Pearson/latent"` 로 덮어쓴 뒤 `"Spearman"` 을 찾는
  코드였다. 그래서 "주분석과 동일"이라는 종전 보고는 **버그 산물**이었다.
- 실제로 Spearman 2건을 빼면 **k=4, r=+0.484, p=.028**.

**왜 이것을 본문에 싣는가.** 네 건 모두 결론을 뒤집지 않지만, ①은 Methods 의 사실 진술이
틀렸던 것이고 ③은 원문에 없는 값을 만든 것이다. 어느 쪽도 조용히 고칠 성질이 아니다.
동시에 이 사건은 **같은 공식을 여러 파일에 복제하면 반드시 갈라진다**는 것과,
**작성자가 아닌 검증자를 붙여야 잡힌다**는 것을 보여준다(§4.4 방법론 함의에 반영).

---

## 요약 — 심사자가 물을 만한 것

1. **"등록보다 넓게 포함한 것 아닌가"** → D1-1은 배제를 늦춘 것이지 기준을 바꾼 것이 아니다.
   최종 판정은 등록된 PICO로 했고, 경계 3원칙(D1-2)은 오히려 배제 방향으로 작동했다.
2. **"인용추적을 다 안 봤다"** → D1-3에 필터 규칙·건수·한계를 공개했고, 배정 결과 전건을 파일로 남겼다.
3. **"RoB 2는 어디 갔나"** → D2-2. 대상이 2편뿐이라 도구를 늘려도 분석이 성립하지 않는다.
4. **"클러스터가 줄었다"** → D3-1. 자료 부족이며, 해당 연구들은 서술 종합에 그대로 남아 있다.
