# 새 세션 복붙 프롬프트 — paper32 원고 집필

아래 코드블록 전체를 새 세션 첫 메시지로 붙여넣으세요.

```
paper32(사운드스케이프·행태 리뷰 논문, LUP 투고) 원고 집필을 이어서 합니다.

핸드오프 문서를 먼저 읽으세요: @_internal/handoff/active/handoff_20260815_220900_paper32_manuscript.md

컨텍스트: 코퍼스 98편 확정, 메타분석 4클러스터 완료, 한글 원고 초안 완성(본문 8,747 어절),
번호 인용 [N] 전환 완료, 그림 8종 재작성 완료. 정본은 마크다운이고 Word 는 빌더가 생성합니다.

불변 제약:
- 정본 원고 = 01_논문작업/Manuscript_KO_20260806.md (docx 직접 편집 금지, build_docx.py 사용)
- 인용은 번호 [N] 방식 · 제목·절제목·표·그림은 영문, 산문만 한글
- 수치는 코드에 하드코딩 금지(figures/fig_data.json 이 유일 소스)
- 인용 문헌은 Crossref 실재 확인 후에만 사용(지어내지 말 것)
- git add -A 금지 · push 는 명시 요청 시만

첫 행동:
1. git status -sb 와 ls "assets/32_행태_사운드스케이프 리뷰 논문_(LUP)/01_논문작업/"*.docx 로 현재 상태 재실측
2. 최신 docx 에 사용자 새 메모가 달렸는지 확인(unzip -l | grep comments)
3. 메모가 있으면 전문 추출해 대응, 없으면 핸드오프 §6 의 T2(Results 인용 보강)부터

미완 task 는 핸드오프 §6 에 T1~T5 로 정리돼 있습니다. T3(영문본 동기화)·T4(Supplementary
영문화)는 한글본 확정 후로 미뤄 둔 상태이고, T5(ChatGPT 검증)는 macmini 로그인이 필요한
사용자 결정 사항입니다.
```

## 붙여넣기 함정

여러 줄을 한 번에 붙여넣으면 `[Pasted text #N]` 으로 접혀 잘못 전송될 수 있습니다.
접힘이 생기면 그대로 두고 전송해도 됩니다(슬래시 명령이 아니므로 파싱 문제 없음).
